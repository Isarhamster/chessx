CNAME=chessx-1.4.4
__SCRIPTDIR="${0%/*}"

if [ -d "$CNAME" ]; then
   tar -czf "${CNAME}.tgz" --exclude-from=$__SCRIPTDIR/tar_exclude.txt ./${CNAME}
   echo "Ready"
else
  echo "Directory not found"
fi




